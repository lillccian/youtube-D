<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <!-- <a class="navbar-brand" href='' data-turbolinks ="false">YTP:D</a> -->
      <%= link_to 'YouTube:D', songs_path, :class=>'navbar-brand glyphicon glyphicon-tree-conifer' %> 
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <% if current_user %>
          <li><img src="<%=current_user.photo%>" alt="" class="photo"></li>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"><%= current_user.name? %><span class="caret"></span></a>
            <ul class="dropdown-menu">
              <li><%= link_to('Edit Profile', edit_registration_path(:user)) %></li>
              <li><%= link_to('Sign out', destroy_user_session_path, :method => :delete) %></li>
              <% if current_user.admin? %>
                <li><%= link_to "Backstage",admin_users_path %></li>
              <% end %>
            </ul>
          <% else %> 
            <li><%= link_to('Sign in', new_session_path(:user)) %></li>
            <li><%= link_to "Sign By FB", user_facebook_omniauth_authorize_path %></li>
            <li><%= link_to('Sign up', new_registration_path(:user)) %></li>
          </li>
        <% end %>
      </ul>

      <form class="navbar-form navbar-right">
        <div class="form-group"><%= form_tag songs_path, :method => :get do %>
          <%= text_field_tag 'keyword', nil,:class=>"form-control", placeholder: 'Search By Tag' %>
          <button type="submit" class ="btn btn-default glyphicon glyphicon-search
"></button>
          <%= link_to '',songs_path,:class=>"btn btn-default glyphicon glyphicon-refresh" %>
        <% end %>
      </form>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>